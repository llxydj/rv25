# RVOIS - Operational Framework & System Architecture

## 📐 System Overview

**RVOIS (Rescue Volunteers Operations Information System)** is a Next.js 14 full-stack application with Supabase as the backend-as-a-service platform, providing real-time emergency response coordination for Talisay City, Negros Occidental.

---

## 🚀 Quick Reference: User Interaction Flows

### 👤 **RESIDENT** Flow
```
Login (Google OAuth)
  ↓
Profile Check → Complete? → NO → /resident/register-google
  ↓ YES
PIN Check → Has PIN? → NO → /pin/setup
  ↓ YES
PIN Verify → /pin/verify
  ↓
/resident/dashboard
  ↓
Report Incident → POST /api/incidents
  ↓
Supabase DB → Triggers → SMS/Push Notifications
  ↓
View Status → GET /api/incidents/[id]
```

### 👨‍💼 **ADMIN** Flow
```
Login (Email/Password or Google)
  ↓
PIN Check → Verify PIN
  ↓
/admin/dashboard
  ↓
View Incidents → GET /api/admin/incidents
  ↓
Assign to Volunteer → POST /api/admin/incidents/assign
  ↓
Monitor System → GET /api/admin/analytics/*
  ↓
Manage Users → POST /api/admin/users/*
```

### 🚑 **VOLUNTEER** Flow
```
Login (Email/Password or Google)
  ↓
PIN Check → Verify PIN
  ↓
/volunteer/dashboard
  ↓
View Assigned Incidents → GET /api/volunteer/incidents
  ↓
Update Location → POST /api/volunteer/location/track
  ↓
Accept/Resolve → POST /api/volunteer/incidents/[id]/resolve
  ↓
Log Activity → POST /api/volunteer/activities
```

### 🏘️ **BARANGAY** Flow
```
Login (Email/Password)
  ↓
/barangay/dashboard
  ↓
View Barangay Incidents → GET /api/barangays (filtered by barangay)
  ↓
Create Incident → POST /api/incidents
  ↓
Update Status → PUT /api/incidents/[id]
```

---

## 🏗️ Architecture Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │   Browser    │  │   Mobile     │  │   PWA       │           │
│  │   (Web)      │  │   Browser    │  │   (App)     │           │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘           │
│         │                  │                  │                  │
│         └──────────────────┴──────────────────┘                  │
│                            │                                     │
│                    ┌───────▼────────┐                            │
│                    │  Next.js App  │                            │
│                    │   (Frontend)  │                            │
│                    └───────┬───────┘                            │
└────────────────────────────┼────────────────────────────────────┘
                              │
┌─────────────────────────────▼────────────────────────────────────┐
│                    API LAYER (Next.js API Routes)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  /api/admin  │  │ /api/resident │  │/api/volunteer│         │
│  │  /api/incidents│ │  /api/pin    │  │ /api/sms     │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                  │
└─────────┼──────────────────┼──────────────────┼─────────────────┘
          │                  │                  │
┌─────────▼──────────────────▼──────────────────▼─────────────────┐
│                    SUPABASE CLOUD                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Auth       │  │  Database    │  │   Storage    │          │
│  │  (OAuth)     │  │  (PostgreSQL)│  │  (Files)     │          │
│  │  - Google    │  │  - RLS       │  │  - Photos    │          │
│  │  - Email     │  │  - Triggers  │  │  - Documents │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐                            │
│  │   Realtime   │  │   Edge       │                            │
│  │  (WebSocket) │  │  Functions   │                            │
│  └──────────────┘  └──────────────┘                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 👥 User Roles & Access Matrix

| Feature | Admin | Volunteer | Resident | Barangay |
|---------|-------|-----------|----------|----------|
| **View Incidents** | ✅ All | ✅ Assigned/Area | ✅ Own Reports | ✅ Barangay Only |
| **Create Incidents** | ✅ | ❌ | ✅ | ✅ |
| **Assign Incidents** | ✅ | ❌ | ❌ | ❌ |
| **Resolve Incidents** | ✅ | ✅ | ❌ | ❌ |
| **Manage Users** | ✅ | ❌ | ❌ | ❌ |
| **View Analytics** | ✅ Full | ✅ Personal | ❌ | ✅ Barangay |
| **SMS Notifications** | ✅ | ✅ | ✅ | ✅ |
| **Push Notifications** | ✅ | ✅ | ✅ | ❌ |
| **PIN Security** | ✅ | ✅ | ✅ | ❌ |

---

## 🔄 End-to-End Data Flow with Supabase

### Request Flow (Client → Server → Database)
```
┌─────────────┐
│   CLIENT    │  (Browser/React Component)
│  Component  │
└──────┬──────┘
       │
       │ 1. User Action (e.g., Submit Form)
       │    fetch('/api/incidents', { method: 'POST', body: {...} })
       │
       ▼
┌─────────────────────────────────────┐
│  Next.js API Route                  │
│  /api/incidents/route.ts            │
│  ┌──────────────────────────────┐   │
│  │ - Validate request           │   │
│  │ - Check authentication       │   │
│  │ - Rate limiting              │   │
│  │ - Input validation (Zod)      │   │
│  └──────────────────────────────┘   │
└──────┬──────────────────────────────┘
       │
       │ 2. getServerSupabase()
       │    - Reads cookies() from request
       │    - Creates Supabase client with session
       │
       ▼
┌─────────────────────────────────────┐
│  Supabase Client                    │
│  (Server-side with RLS)             │
│  ┌──────────────────────────────┐   │
│  │ supabase.from('incidents')   │   │
│  │   .insert({...})             │   │
│  │   .select()                  │   │
│  └──────────────────────────────┘   │
└──────┬──────────────────────────────┘
       │
       │ 3. SQL Query (with RLS policies)
       │    INSERT INTO incidents ...
       │
       ▼
┌─────────────────────────────────────┐
│  Supabase PostgreSQL Database       │
│  ┌──────────────────────────────┐   │
│  │ - RLS Policy Check           │   │
│  │ - Insert Record              │   │
│  │ - Trigger Fires              │   │
│  │   └─► notify_admins()       │   │
│  │   └─► create_notification()  │   │
│  └──────────────────────────────┘   │
└──────┬──────────────────────────────┘
       │
       │ 4. Database Triggers
       │
       ▼
┌─────────────────────────────────────┐
│  Background Processes                │
│  ┌──────────────────────────────┐   │
│  │ - SMS Service (async)        │   │
│  │ - Push Notifications         │   │
│  │ - Realtime Broadcast         │   │
│  └──────────────────────────────┘   │
└──────┬──────────────────────────────┘
       │
       │ 5. Response to Client
       │
       ▼
┌─────────────┐
│   CLIENT    │  Receives JSON response
│  Component  │  Updates UI state
└─────────────┘
```

### Real-Time Update Flow (Database → Client)
```
┌─────────────────────────────────────┐
│  Supabase Database                  │
│  - INSERT/UPDATE/DELETE             │
└──────┬──────────────────────────────┘
       │
       │ 1. Change detected
       │
       ▼
┌─────────────────────────────────────┐
│  Supabase Realtime                  │
│  - WebSocket connection              │
│  - Channel: 'incidents'              │
│  - Event: 'INSERT'                  │
└──────┬──────────────────────────────┘
       │
       │ 2. Broadcast to subscribed clients
       │
       ▼
┌─────────────────────────────────────┐
│  Client Subscriptions                │
│  ┌──────────────────────────────┐   │
│  │ supabase.channel('incidents')│   │
│  │   .on('INSERT', callback)    │   │
│  │   .subscribe()               │   │
│  └──────────────────────────────┘   │
└──────┬──────────────────────────────┘
       │
       │ 3. Event received
       │
       ▼
┌─────────────┐
│   CLIENT    │  Updates UI in real-time
│  Component  │  Shows notification
└─────────────┘
```

## 🔄 Complete Data Flow: Resident Reports Incident

```
┌─────────────┐
│   RESIDENT  │
│  (Browser)  │
└──────┬──────┘
       │
       │ 1. User fills incident form
       │    - Type, Description, Location
       │    - Photos, Voice recording
       │
       ▼
┌─────────────────────────────────────┐
│  Frontend: /resident/report         │
│  - Form validation                  │
│  - Photo upload to Supabase Storage │
│  - Location geocoding               │
└──────┬──────────────────────────────┘
       │
       │ 2. POST /api/incidents
       │
       ▼
┌─────────────────────────────────────┐
│  API Route: /api/incidents          │
│  - Validate payload                │
│  - Check geofence (Talisay City)   │
│  - Rate limiting                    │
└──────┬──────────────────────────────┘
       │
       │ 3. Insert into Supabase
       │
       ▼
┌─────────────────────────────────────┐
│  SUPABASE DATABASE                  │
│  ┌──────────────────────────────┐  │
│  │ incidents table               │  │
│  │ - id, type, status, location  │  │
│  │ - reporter_id, assigned_to    │  │
│  │ - created_at, priority        │  │
│  └──────────────────────────────┘  │
│                                     │
│  ┌──────────────────────────────┐  │
│  │ Database Triggers            │  │
│  │ - notify_admins_on_new_incident│ │
│  │ - notify_barangay_on_new_incident││
│  └──────────────────────────────┘  │
└──────┬──────────────────────────────┘
       │
       │ 4. Triggers fire → Create notifications
       │
       ▼
┌─────────────────────────────────────┐
│  BACKGROUND PROCESSES                │
│  ┌──────────────────────────────┐  │
│  │ SMS Service                  │  │
│  │ - Send to Resident (confirm) │  │
│  │ - Send to Admins (alert)     │  │
│  │ - Send to Barangay (alert)   │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Push Notifications           │  │
│  │ - Web Push to Admins         │  │
│  │ - VAPID keys                 │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ Auto-Assignment Service      │  │
│  │ - Find available volunteer   │  │
│  │ - Match skills & location    │  │
│  │ - Assign incident            │  │
│  └──────────────────────────────┘  │
└─────────────────────────────────────┘
       │
       │ 5. Real-time updates via Supabase Realtime
       │
       ▼
┌─────────────────────────────────────┐
│  NOTIFIED USERS                     │
│  ┌──────────┐  ┌──────────┐        │
│  │  ADMIN   │  │VOLUNTEER │        │
│  │  (Push)  │  │  (SMS)   │        │
│  └──────────┘  └──────────┘        │
└─────────────────────────────────────┘
```

---

## 🔐 Authentication & Authorization Flow

```
┌─────────────┐
│   USER      │
└──────┬──────┘
       │
       │ 1. Login Request
       │    - Email/Password OR
       │    - Google OAuth
       │
       ▼
┌─────────────────────────────────────┐
│  Supabase Auth                      │
│  - Verify credentials               │
│  - Create session                   │
│  - Return JWT token                 │
└──────┬──────────────────────────────┘
       │
       │ 2. Session established
       │
       ▼
┌─────────────────────────────────────┐
│  /auth/callback                     │
│  - Exchange OAuth code              │
│  - Check user profile               │
│  - Validate completeness           │
└──────┬──────────────────────────────┘
       │
       │ 3. Profile check
       │
       ▼
┌─────────────────────────────────────┐
│  Profile Validation                 │
│  ┌──────────────────────────────┐  │
│  │ Complete?                    │  │
│  │ - first_name, last_name      │  │
│  │ - phone_number, address      │  │
│  │ - barangay                   │  │
│  └──────────────────────────────┘  │
│         │                            │
│    NO   │  YES                      │
│    │    │                            │
│    ▼    ▼                            │
│  ┌──────────┐  ┌──────────┐         │
│  │Register  │  │PIN Check │         │
│  │Page      │  │          │         │
│  └──────────┘  └────┬─────┘         │
└─────────────────────┼───────────────┘
                      │
                      │ 4. PIN Security
                      │
                      ▼
┌─────────────────────────────────────┐
│  PIN Security Gate                  │
│  ┌──────────────────────────────┐  │
│  │ Has PIN?                     │  │
│  │ - NO  → /pin/setup          │  │
│  │ - YES → /pin/verify         │  │
│  └──────────────────────────────┘  │
└──────┬──────────────────────────────┘
       │
       │ 5. PIN verified → Set cookie
       │
       ▼
┌─────────────────────────────────────┐
│  Role-Based Dashboard                │
│  - /admin/dashboard                  │
│  - /volunteer/dashboard              │
│  - /resident/dashboard                │
│  - /barangay/dashboard                │
└─────────────────────────────────────┘
```

---

## 📊 Database Schema (Key Tables)

```
┌─────────────────────────────────────────────────────────────┐
│                    SUPABASE DATABASE                        │
│                                                             │
│  ┌──────────────┐      ┌──────────────┐                    │
│  │   users      │      │  incidents   │                    │
│  │──────────────│      │──────────────│                    │
│  │ id (PK)      │◄─────│ reporter_id │                    │
│  │ email        │      │ assigned_to │                    │
│  │ role         │      │ type         │                    │
│  │ pin_hash     │      │ status       │                    │
│  │ pin_enabled  │      │ location     │                    │
│  └──────────────┘      └──────┬───────┘                    │
│                               │                             │
│  ┌──────────────┐      ┌──────▼───────┐                    │
│  │volunteer_    │      │notifications │                    │
│  │profiles      │      │──────────────│                    │
│  │──────────────│      │ incident_id  │                    │
│  │volunteer_    │      │ user_id      │                    │
│  │user_id (FK)  │      │ type         │                    │
│  │ skills       │      │ read         │                    │
│  │ status       │      └──────────────┘                    │
│  └──────────────┘                                           │
│                                                             │
│  ┌──────────────┐      ┌──────────────┐                    │
│  │sms_logs      │      │push_         │                    │
│  │──────────────│      │subscriptions │                    │
│  │ incident_id  │      │──────────────│                    │
│  │ user_id      │      │ user_id      │                    │
│  │ status       │      │ subscription │                    │
│  └──────────────┘      └──────────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔔 Notification System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│              NOTIFICATION DELIVERY SYSTEM                    │
│                                                             │
│  Event: New Incident Created                                │
│         │                                                    │
│         ├─► Database Trigger                                │
│         │   └─► Create notification records                 │
│         │                                                    │
│         ├─► Push Notification Service                       │
│         │   ├─► Get admin subscriptions                    │
│         │   ├─► Send via Web Push API                      │
│         │   └─► VAPID keys authentication                  │
│         │                                                    │
│         ├─► SMS Service                                     │
│         │   ├─► Resident confirmation                       │
│         │   ├─► Admin critical alert                       │
│         │   ├─► Barangay alert                             │
│         │   └─► iProgSMS API (639XXXXXXXXX)                │
│         │                                                    │
│         └─► Realtime Channel                                │
│             └─► Supabase Realtime (WebSocket)              │
│                 └─► Live updates to connected clients      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🛡️ Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY STACK                           │
│                                                             │
│  Layer 1: Authentication                                    │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ - Supabase Auth (JWT tokens)                          │ │
│  │ - Google OAuth integration                            │ │
│  │ - Email/Password with bcrypt                          │ │
│  │ - Session management (HTTP-only cookies)              │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                             │
│  Layer 2: Authorization                                    │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ - Role-Based Access Control (RBAC)                   │ │
│  │ - Row Level Security (RLS) in Supabase               │ │
│  │ - API route role checks                               │ │
│  │ - PIN verification (4-digit)                          │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                             │
│  Layer 3: Data Protection                                  │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ - Rate limiting (per endpoint)                        │ │
│  │ - Input validation (Zod schemas)                     │ │
│  │ - Geofencing (Talisay City bounds)                   │ │
│  │ - SQL injection protection (Supabase)                │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                             │
│  Layer 4: Communication Security                           │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ - HTTPS/TLS (production)                             │ │
│  │ - VAPID keys for push notifications                   │ │
│  │ - Secure cookie flags (httpOnly, secure, sameSite)   │ │
│  └──────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

---

## 📱 API Endpoint Structure

### Admin Endpoints (`/api/admin/*`)
```
/admin/incidents/assign     → Assign incident to volunteer
/admin/analytics/*          → System analytics & metrics
/admin/users/*             → User management (CRUD)
/admin/volunteers/*         → Volunteer management
/admin/reports/*            → Generate reports
/admin/sms                  → SMS monitoring
```

### Resident Endpoints (`/api/resident/*`)
```
/resident/register-google   → Complete Google OAuth profile
```

### Volunteer Endpoints (`/api/volunteer/*`)
```
/volunteer/location/*        → Location tracking
/volunteer/incidents/*       → View assigned incidents
/volunteer/activities/*      → Activity logs
```

### Shared Endpoints (`/api/*`)
```
/incidents                   → Create/Read incidents
/notifications               → Notification management
/pin/*                       → PIN security
/sms/*                       → SMS operations
/announcements               → Public announcements
```

---

## 🔄 Real-Time Updates Flow

```
┌─────────────┐
│   CLIENT    │
└──────┬──────┘
       │
       │ 1. Subscribe to Supabase Channel
       │    supabase.channel('incidents')
       │      .on('INSERT', callback)
       │
       ▼
┌─────────────────────────────────────┐
│  Supabase Realtime                  │
│  - WebSocket connection              │
│  - Channel subscriptions             │
│  - Event filtering (RLS)            │
└──────┬──────────────────────────────┘
       │
       │ 2. Database change detected
       │
       ▼
┌─────────────────────────────────────┐
│  Database Trigger                   │
│  - INSERT/UPDATE on incidents        │
│  - Broadcast to subscribed clients   │
└──────┬──────────────────────────────┘
       │
       │ 3. Event pushed to client
       │
       ▼
┌─────────────────────────────────────┐
│  Client receives update             │
│  - Update UI state                  │
│  - Show notification                │
│  - Refresh data if needed           │
└─────────────────────────────────────┘
```

---

## 🗄️ Data Storage Architecture

```
┌─────────────────────────────────────────────────────────────┐
│              SUPABASE STORAGE BUCKETS                       │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ incident-photos/                                    │  │
│  │ - uploaded/ (temporary)                             │  │
│  │ - processed/ (final)                                │  │
│  │ - Public URLs for display                            │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ volunteer-documents/                                 │  │
│  │ - Certificates, IDs, etc.                            │  │
│  │ - Private (RLS protected)                            │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ admin-documents/                                      │  │
│  │ - Reports, PDFs                                       │  │
│  │ - Admin-only access                                  │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION STACK                        │
│                                                             │
│  ┌──────────────┐                                          │
│  │   Vercel     │  ← Next.js App (Serverless)             │
│  │  (Hosting)   │     - API Routes                        │
│  │              │     - SSR/SSG Pages                     │
│  └──────┬───────┘     - Edge Functions                    │
│         │                                                    │
│         │ HTTPS                                              │
│         │                                                    │
│         ▼                                                    │
│  ┌──────────────┐                                          │
│  │   Supabase   │  ← Backend Services                      │
│  │   (Cloud)    │     - PostgreSQL Database                │
│  │              │     - Authentication                     │
│  │              │     - Storage                            │
│  │              │     - Realtime                           │
│  └──────────────┘                                          │
│                                                             │
│  ┌──────────────┐                                          │
│  │ iProgSMS API │  ← External SMS Service                  │
│  │  (SMS)       │     - 639XXXXXXXXX format               │
│  └──────────────┘                                          │
│                                                             │
│  ┌──────────────┐                                          │
│  │  Web Push    │  ← Browser Push API                      │
│  │  (VAPID)     │     - Service Worker                    │
│  └──────────────┘                                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Key Services & Utilities

### 1. **SMS Service** (`src/lib/sms-service.ts`)
- **Purpose**: Send SMS notifications via iProgSMS API
- **Features**: Rate limiting, retry logic, template system
- **Flow**: Incident created → SMS sent to resident, admins, barangay

### 2. **Push Notification Service** (`src/lib/push-notification-service.ts`)
- **Purpose**: Web Push notifications for real-time alerts
- **Features**: VAPID keys, subscription management, service worker
- **Flow**: Incident created → Push sent to subscribed admins

### 3. **Auto-Assignment Service** (`src/lib/auto-assignment.ts`)
- **Purpose**: Automatically assign incidents to volunteers
- **Features**: Skill matching, location-based, availability check
- **Flow**: New incident → Find best volunteer → Auto-assign

### 4. **PIN Security** (`src/lib/pin-rate-limit.ts`)
- **Purpose**: 4-digit PIN for additional security
- **Features**: Rate limiting, brute force protection, lockout
- **Flow**: Login → PIN check → Dashboard access

### 5. **Rate Limiting** (`src/lib/rate-limit.ts`)
- **Purpose**: Prevent API abuse
- **Features**: Per-endpoint limits, IP-based tracking
- **Flow**: Request → Check limit → Allow/Deny

---

## 📈 Performance Optimizations

1. **Caching**
   - PIN status: 5-second cache
   - Location data: 10-minute cache
   - Analytics: Aggregated queries

2. **Lazy Loading**
   - Dynamic imports for heavy components
   - Code splitting by route
   - Image optimization

3. **Database**
   - Indexed queries
   - RLS policies for security
   - Connection pooling (Supabase)

4. **API**
   - Rate limiting
   - Request validation
   - Error handling

---

## 🔍 Monitoring & Logging

- **Console Logs**: Development debugging
- **SMS Logs**: `sms_logs` table (delivery status)
- **Activity Logs**: `volunteer_activity_logs` table
- **System Logs**: `system_logs` table (admin actions)
- **Error Tracking**: Try-catch blocks with error messages

---

## 🎯 Critical Paths

### Path 1: Resident Reports Emergency
```
Resident → Form → API → Database → Triggers → 
SMS/Push → Admins → Auto-Assign → Volunteer → Resolve
```

### Path 2: Admin Manages System
```
Admin → Dashboard → Analytics → Reports → 
User Management → Volunteer Assignment → Monitoring
```

### Path 3: Volunteer Responds
```
Volunteer → View Assignment → Accept → 
Location Tracking → Update Status → Resolve → Log Activity
```

---

## 🔐 Security Checklist

✅ **Authentication**: Supabase Auth + Google OAuth  
✅ **Authorization**: Role-based + RLS policies  
✅ **PIN Security**: 4-digit with rate limiting  
✅ **Rate Limiting**: Per-endpoint protection  
✅ **Input Validation**: Zod schemas  
✅ **Geofencing**: Talisay City bounds  
✅ **HTTPS**: Production TLS  
✅ **Secure Cookies**: HTTP-only, secure flags  
✅ **SQL Injection**: Supabase parameterized queries  
✅ **XSS Protection**: React auto-escaping  

---

## 📝 Summary

**RVOIS** is a **3-tier architecture**:
1. **Frontend**: Next.js 14 with React (Client-side)
2. **API Layer**: Next.js API Routes (Serverless functions)
3. **Backend**: Supabase Cloud (Database, Auth, Storage, Realtime)

**Data Flow**: User → Frontend → API Route → Supabase → Database → Triggers → Notifications → Users

**Security**: Multi-layer (Auth → Authorization → PIN → Rate Limiting → Validation)

**Real-time**: Supabase Realtime channels for live updates

**Notifications**: Dual-channel (SMS + Push) for reliability

---

*Last Updated: 2025-01-28*

