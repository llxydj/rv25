import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Add New Volunteer - RVOIS",
  description: "Register a new volunteer in the system.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 