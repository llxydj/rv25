import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "System Settings - RVOIS",
  description: "Configure and manage system settings and preferences.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 