import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Admin Dashboard - RVOIS",
  description: "Administrative dashboard for managing incidents, volunteers, and system operations.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 