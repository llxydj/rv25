import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Create New Incident - RVOIS",
  description: "Create and report a new incident in the system.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 