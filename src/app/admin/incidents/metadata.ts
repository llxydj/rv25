import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Incidents Management - RVOIS",
  description: "View and manage all reported incidents in the system.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 