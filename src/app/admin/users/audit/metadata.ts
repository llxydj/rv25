import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "User Management Audit Log | Admin Dashboard",
  description: "View audit logs for user management activities in the RVOIS Admin Dashboard",
}