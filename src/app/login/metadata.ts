import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Login - RVOIS",
  description: "Sign in to your RVOIS account.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 