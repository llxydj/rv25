import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Register Account - RVOIS",
  description: "Create a new account in the RVOIS system.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 