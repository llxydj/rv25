import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "RVOIS Application",
  description: "Main application interface for RVOIS system.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 