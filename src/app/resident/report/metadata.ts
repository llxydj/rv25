import type { Metadata, Viewport } from "next"

export const metadata: Metadata = {
  title: "Report Incident - RVOIS",
  description: "Submit a new incident report as a resident.",
}

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  themeColor: "#ef4444",
} 